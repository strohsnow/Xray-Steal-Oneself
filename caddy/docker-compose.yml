services:
  caddy:
    image: caddy:2.9.1
    container_name: caddy
    restart: always
    network_mode: host
    env_file: .env
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./index.html:/srv/index.html
      - ./sub:/srv/sub
      - ssl-data:/data

volumes:
  ssl-data:
